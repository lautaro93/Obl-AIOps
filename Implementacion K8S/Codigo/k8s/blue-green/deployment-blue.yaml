apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-blue
  namespace: pharmago
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo
      version: blue
  template:
    metadata:
      labels:
        app: demo
        version: blue
    spec:
      containers:
      - name: http-echo
        image: hashicorp/http-echo
        args:
        - "-text=BLUE"
        - "-listen=:80"
        ports:
        - containerPort: 80
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-green
  namespace: pharmago
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo
      version: green
  template:
    metadata:
      labels:
        app: demo
        version: green
    spec:
      containers:
      - name: http-echo
        image: hashicorp/http-echo
        args:
        - "-text=GREEN"
        - "-listen=:80"
        ports:
        - containerPort: 80